---
import ArrowUpRightIcon from '../../icons/ArrowUpRightIcon.astro';
import CodeIcon from '../../icons/CodeIcon.astro';
import DesignIcon from '../../icons/DesignIcon.astro';
---

<section
  class="relative isolate mt-2 flex flex-col items-center justify-center gap-4 px-5 py-40 text-center md:mt-12 md:gap-6 md:px-2"
>
  <!-- Background grid overlay -->
  <div
    aria-hidden="true"
    class="pointer-events-none absolute inset-x-0 top-0 -z-10 mx-auto h-[330px] w-full max-w-6xl md:h-[380px]"
  >
    <div class="grid-pattern absolute inset-0"></div>
    <!-- Accent squares -->
    <div class="accent-square absolute top-14 left-[12%] h-16 w-16 rounded bg-teal-400/20"></div>
    <div class="accent-square absolute top-6 right-[14%] h-16 w-16 rounded bg-teal-400/20"></div>
    <div
      class="accent-square absolute top-10 right-[34%] hidden h-16 w-16 rounded bg-teal-400/20 sm:block"
    >
    </div>
  </div>
  <div
    class="rounded-full bg-[#F2F7F7] px-4 py-2 text-xs font-medium text-gray-700 shadow-sm md:text-sm"
  >
    <span>+20 PRODUCTS LAUNCHED</span>
  </div>
  <h1
    class="max-w-4xl text-[40px] leading-tight font-semibold tracking-tighter text-balance text-gray-900 md:text-6xl md:leading-tight"
  >
    We design
    <span
      class="hidden h-16 w-16 items-center justify-center rounded-2xl bg-gray-900 align-text-bottom md:inline-flex"
    >
      <DesignIcon class="h-8 w-8" />
    </span> and build digital products
    <span
      class="hidden h-16 w-16 items-center justify-center rounded-2xl bg-gray-900 align-text-bottom md:inline-flex"
    >
      <CodeIcon class="h-8 w-8" />
    </span>
  </h1>
  <p class="max-w-2xl text-base text-gray-600 md:text-lg">
    From strategy to shipping, we partner with startups and tech teams to create scalable,
    user-driven products — fast, and future-ready.
  </p>
  <div class="mt-4 flex gap-4">
    <a
      href="#"
      class="btn btn-neutral btn-md md:btn-lg gap-2 rounded-full text-sm font-normal normal-case transition-transform hover:scale-105 md:font-medium"
    >
      Let's talk
      <ArrowUpRightIcon class="h-4 w-4 md:h-5 md:w-5" />
    </a>
    <a
      href="#"
      class="btn btn-outline btn-md md:btn-lg rounded-full border-gray-300 bg-white text-sm font-normal text-gray-900 normal-case shadow-sm transition-transform hover:scale-105 md:font-medium"
    >
      See our work
    </a>
  </div>
</section>

<style>
  .grid-pattern {
    --grid-line-color: rgba(2, 6, 23, 0.06);
    /* Use non-repeating gradients with explicit background-size to avoid near-overlapping lines on small screens */
    background-image:
      linear-gradient(to right, var(--grid-line-color) 0 1px, transparent 1px),
      linear-gradient(to bottom, var(--grid-line-color) 0 1px, transparent 1px);
    background-size: 64px 64px;
    background-repeat: repeat;
    /* Offset the grid so no line sits exactly on top/left edges */
    background-position:
      32px 0,
      0 32px;
    /* Fade top/bottom (earlier at the bottom) + softly fade on the left edge */
    -webkit-mask-image:
      linear-gradient(
        to bottom,
        transparent 0%,
        black 12%,
        black 60%,
        transparent 92%,
        transparent 100%
      ),
      linear-gradient(to right, transparent 0%, black 8%, black 100%);
    mask-image:
      linear-gradient(
        to bottom,
        transparent 0%,
        black 12%,
        black 60%,
        transparent 92%,
        transparent 100%
      ),
      linear-gradient(to right, transparent 0%, black 8%, black 100%);
    /* Intersect both masks (broad support via WebKit prefixed + spec) */
    -webkit-mask-composite: source-over, source-in;
    mask-composite: add, intersect;
  }

  .accent-square {
    box-shadow: 0 14px 28px rgba(2, 6, 23, 0.16);
  }
</style>
