---
// src/components/astro/Navbar.astro

import MobileMenu from '../react/MobileMenu';

interface MenuItem {
  href: string;
  label: string;
  isActive?: boolean;
}

const menuItems: MenuItem[] = [
  { href: '/', label: 'Home' },
  { href: '/about', label: 'Work' },
  { href: '/projects', label: 'About' },
  { href: '/contact', label: 'Careers' },
  { href: '/contact', label: 'Blog' },
];

const currentPath = Astro.url.pathname;
---

<nav class="flex items-center justify-between border-b border-gray-300 px-15 py-6">
  <a href="/" class="flex items-center">
    <img src="/assets/logo.png" alt="Logo" class="h-8 w-auto" />
  </a>

  <ul class="hidden items-center gap-6 md:flex">
    {
      menuItems.map((item) => (
        <li>
          <a
            href={item.href}
            class={`text-base font-normal transition-colors duration-300 ease-in-out hover:text-black ${
              currentPath === item.href ? 'font-semibold text-black' : 'text-gray-custom'
            }`}
          >
            {item.label}
          </a>
        </li>
      ))
    }
  </ul>

  <a href="/contact" class="btn btn-neutral hidden cursor-pointer justify-center md:flex">
    Let's talk
  </a>

  <MobileMenu client:load menuItems={menuItems} currentPath={currentPath} />
</nav>
